#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Frontend pre-commit checks
cd frontend

# Run linting
npm run lint:check

# Run tests
npm test -- --passWithNoTests

# Run type checking (if TypeScript is enabled)
# npm run type-check

# Run format check
npm run format:check
